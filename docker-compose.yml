services:
  personal-data-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: personal-data-ai
    ports:
      - "8080:8080"
    env_file:
      - .env.local
    restart: unless-stopped
